cmake_minimum_required(VERSION 3.19)
project("vulkan-shaders-gen" C CXX)

find_package (Threads REQUIRED)

if (GGML_VULKAN_COOPMAT_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_COOPMAT_GLSLC_SUPPORT)
    message(STATUS "Enabling coopmat glslc support")
endif()
if (GGML_VULKAN_COOPMAT2_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_COOPMAT2_GLSLC_SUPPORT)
    message(STATUS "Enabling coopmat2 glslc support")
endif()
if (GGML_VULKAN_COOPMAT2_DECODE_VECTOR_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_COOPMAT2_DECODE_VECTOR_GLSLC_SUPPORT)
    message(STATUS "Enabling coopmat2 decode_vector glslc support")
endif()
if (GGML_VULKAN_INTEGER_DOT_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_INTEGER_DOT_GLSLC_SUPPORT)
    message(STATUS "Enabling dot glslc support")
endif()
if (GGML_VULKAN_BFLOAT16_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_BFLOAT16_GLSLC_SUPPORT)
    message(STATUS "Enabling bfloat16 glslc support")
endif()
if (GGML_VULKAN_FLOAT_E2M1_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_FLOAT_E2M1_GLSLC_SUPPORT)
    message(STATUS "Enabling E2M1 glslc support")
endif()
if (GGML_VULKAN_FLOAT_E4M3_GLSLC_SUPPORT)
    add_compile_definitions(GGML_VULKAN_FLOAT_E4M3_GLSLC_SUPPORT)
    message(STATUS "Enabling E4M3 glslc support")
endif()
if (GGML_VULKAN_B390_TUNED)
    add_compile_definitions(GGML_VULKAN_B390_TUNED)
    message(STATUS "Enabling B390 tuned shader routing")
endif()
if (GGML_VULKAN_SHADER_DEBUG_INFO)
    add_compile_definitions(GGML_VULKAN_SHADER_DEBUG_INFO)
    message(STATUS "Enabling shader debug info")
endif()
if (GGML_VULKAN_B390_GEMM_CACHE_SCOPE)
    add_compile_definitions(GGML_VULKAN_B390_GEMM_CACHE_SCOPE)
    message(STATUS "Enabling B390 Q4_K/Q5_K GEMM cache-scope candidate")
endif()
if (GGML_VULKAN_B390_Q4_BITFIELD_DEQUANT)
    add_compile_definitions(GGML_VULKAN_B390_Q4_BITFIELD_DEQUANT)
    message(STATUS "Enabling B390 Q4_K-only bitfield dequant candidate")
endif()
if (GGML_VULKAN_B390_Q5_BITFIELD_DEQUANT)
    add_compile_definitions(GGML_VULKAN_B390_Q5_BITFIELD_DEQUANT)
    message(STATUS "Enabling B390 Q5_K-only bitfield dequant candidate")
endif()
if (GGML_VULKAN_B390_ROUTE_COUNT_WG128)
    add_compile_definitions(GGML_VULKAN_B390_ROUTE_COUNT_WG128)
    message(STATUS "Enabling B390 MoE route-count 128-lane candidate")
endif()
if (GGML_VULKAN_B390_ROUTE_SINGLE_SCAN)
    add_compile_definitions(GGML_VULKAN_B390_ROUTE_SINGLE_SCAN)
    message(STATUS "Enabling B390 MoE single-scan route-count candidate")
endif()
if (GGML_VULKAN_B390_ROUTE_HISTOGRAM_SCAN)
    add_compile_definitions(GGML_VULKAN_B390_ROUTE_HISTOGRAM_SCAN)
    message(STATUS "Enabling B390 MoE block-histogram route-count candidate")
endif()
if (GGML_VULKAN_B390_ROUTE_INDIRECT)
    add_compile_definitions(GGML_VULKAN_B390_ROUTE_INDIRECT)
    message(STATUS "Enabling B390 active-expert indirect-dispatch candidate")
endif()
set(TARGET vulkan-shaders-gen)
add_executable(${TARGET} vulkan-shaders-gen.cpp)
install(TARGETS ${TARGET} RUNTIME)
target_compile_features(${TARGET} PRIVATE cxx_std_17)
target_link_libraries(vulkan-shaders-gen PUBLIC Threads::Threads)
